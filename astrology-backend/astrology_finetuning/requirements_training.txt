# Training dependencies for Local QLoRA Fine-tuning (16GB VRAM)
# Recommended: Install in a dedicated clean environment (Python 3.10 or 3.11)

# PyTorch with CUDA 12.1+ support (install this first via: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121)
torch>=2.3.0

# Unsloth for 2x-5x faster training with 60% less VRAM
unsloth>=2024.8

# Core ML Libraries
transformers>=4.45.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.9.6
accelerate>=0.33.0
bitsandbytes>=0.43.0

# GGUF / Llama.cpp for CPU inference testing
llama-cpp-python>=0.2.89

# Tracking & Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
